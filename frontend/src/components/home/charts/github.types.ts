export interface LanguageRepositoryCount {
  name: string;
  count: number;
  color: string;
}

export interface LanguageRepositoryBytes {
  name: string;
  bytes: number;
  color: string;
}

export interface GitHubCommitCount {
  date: string;
  count: number;
  total: number;
}
