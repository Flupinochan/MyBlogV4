---
import SectionHeading from "../../shared/SectionHeading.astro";
import LinkButton from "../../shared/LinkButton.astro";
import User from "../../icons/User.astro";
import Briefcase from "../../icons/Briefcase.astro";
import Star from "../../icons/Star.astro";

const items = [
  {
    icon: User,
    label: "自己紹介",
    body: "面接の時に利用する自己紹介文を記載",
    iconWrapClass: "bg-cyan-500/10",
    iconTextClass: "text-cyan-600 dark:text-cyan-400",
  },
  {
    icon: Briefcase,
    label: "直近の業務",
    body: "claude codeを利用したバグ修正、パフォーマンス改善、開発環境整備",
    iconWrapClass: "bg-amber-500/10",
    iconTextClass: "text-amber-600 dark:text-amber-400",
  },
  {
    icon: Star,
    label: "趣味",
    body: "オクトパストラベラーとK8s/Tailscaleを利用した自宅サーバ構築",
    iconWrapClass: "bg-emerald-500/10",
    iconTextClass: "text-emerald-600 dark:text-emerald-400",
  },
];

const contents = [
  {
    label: "Blog",
    date: "2024-01-28",
    description: "AWS SAMを使用したCI/CDの解説",
    image: "/blog.avif",
    url: "https://zenn.dev/metalmental",
    cta: "Go Blog",
  },
  {
    label: "Youtube",
    date: "2023-11-01",
    description: "ECS Blue/Green Deploymentのゆっくり実況",
    image: "/youtube.avif",
    url: "https://www.youtube.com/@Flupinochan",
    cta: "Go Youtube",
  },
];
---

<SectionHeading step={1} class="mb-16 scroll-mt-16" data-summary-title
  >See Me in Summary</SectionHeading
>

<div class="divide-y divide-slate-300 dark:divide-slate-700 px-10">
  {
    items.map((item) => (
      <div class="summary-row flex gap-4 items-center py-6 opacity-0 invisible">
        <div
          class={`size-9 shrink-0 flex items-center justify-center rounded-full ${item.iconWrapClass} ${item.iconTextClass}`}
        >
          <item.icon />
        </div>
        <div>
          <p class="text-slate-800 dark:text-slate-100 font-bold">
            {item.label}
          </p>
          <p class="text-sm mt-2 text-slate-700/80 dark:text-slate-300/80 leading-relaxed">
            {item.body}
          </p>
        </div>
      </div>
    ))
  }
</div>

<div
  class="grid grid-cols-1 md:grid-cols-2 gap-y-16 md:gap-y-0 md:gap-x-8 px-6 pt-16"
>
  {
    contents.map((content) => (
      <div class="summary-content opacity-0 invisible">
        <p class="text-lg text-slate-800 dark:text-slate-100 font-bold mb-8">
          {content.label}
        </p>
        <div class="flex flex-col gap-5">
          <img
            src={content.image}
            alt={`${content.label}のサムネイル`}
            class="w-full h-auto rounded-xl"
          />
          <p class="text-xs text-slate-400 dark:text-slate-500">
            {content.date}
          </p>
          <p class="text-sm text-slate-700/80 dark:text-slate-300/80 leading-relaxed">
            {content.description}
          </p>
          <LinkButton href={content.url} class="self-start">
            {content.cta}
          </LinkButton>
        </div>
      </div>
    ))
  }
</div>

<script>
  import { initSectionHeadingAnimation } from "../../../lib/sectionHeadingAnimation";

  const summaryTitleEl = document.querySelector("[data-summary-title]");
  if (!summaryTitleEl) {
    throw new Error("Summary title element not found");
  }

  const tl = initSectionHeadingAnimation(summaryTitleEl);

  tl.to(
    ".summary-row",
    {
      autoAlpha: 1,
      stagger: 0.1,
      duration: 0.25,
      ease: "power2.out",
    },
    "-=0.2",
  ).to(".summary-content", {
    autoAlpha: 1,
    stagger: 0.15,
    duration: 0.3,
    ease: "power2.out",
  });
</script>
