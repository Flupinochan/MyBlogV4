---
import SectionHeading from "../../shared/SectionHeading.astro";
import User from "../../icons/User.astro";
import Briefcase from "../../icons/Briefcase.astro";
import Star from "../../icons/Star.astro";

const items = [
  {
    icon: User,
    label: "自己紹介",
    body: "面接の時に利用する自己紹介文を記載",
    iconWrapClass: "bg-cyan-500/10",
    iconTextClass: "text-cyan-600 dark:text-cyan-400",
  },
  {
    icon: Briefcase,
    label: "直近の業務",
    body: "claude codeを利用したバグ修正、パフォーマンス改善、開発環境整備",
    iconWrapClass: "bg-amber-500/10",
    iconTextClass: "text-amber-600 dark:text-amber-400",
  },
  {
    icon: Star,
    label: "趣味",
    body: "オクトパストラベラーとK8s/Tailscaleを利用した自宅サーバ構築",
    iconWrapClass: "bg-emerald-500/10",
    iconTextClass: "text-emerald-600 dark:text-emerald-400",
  },
];
---

<SectionHeading step={1} class="mb-16 scroll-mt-16" data-summary-title
  >See Me in Summary</SectionHeading
>

<div class="divide-y divide-slate-300 dark:divide-slate-700 px-20">
  {
    items.map((item) => (
      <div class="summary-row flex gap-4 items-start py-6 opacity-0 invisible">
        <div
          class={`size-9 shrink-0 flex items-center justify-center rounded-full ${item.iconWrapClass} ${item.iconTextClass}`}
        >
          <item.icon />
        </div>
        <div>
          <p class="text-slate-800 dark:text-slate-100 font-bold">
            {item.label}
          </p>
          <p class="text-sm mt-2 text-slate-700/80 dark:text-slate-300/80 leading-relaxed">
            {item.body}
          </p>
        </div>
      </div>
    ))
  }
</div>

<script>
  import { initSectionHeadingAnimation } from "../../../lib/sectionHeadingAnimation";

  const summaryTitleEl = document.querySelector("[data-summary-title]");
  if (!summaryTitleEl) {
    throw new Error("Summary title element not found");
  }

  const tl = initSectionHeadingAnimation(summaryTitleEl);

  tl.to(
    ".summary-row",
    {
      autoAlpha: 1,
      stagger: 0.1,
      duration: 0.25,
      ease: "power2.out",
    },
    "-=0.2",
  );
</script>
